console.log("HEllo Coder Army");

//* setTimeout, setInterval, console, DOM, eventListener, window object, fetch etc. are not the part of JS engine or JS, instead it is part of WebAPI, which is presented inside browser. & the browser is multi threaded means it can handle multiple task simultaneously.
setTimeout(() => {
  const a = 2 * 4;
  console.log(a);
}, 5000);

let b = 20;
let arr = [20, 30, 11];

for (let i of arr) console.log(i * b);
